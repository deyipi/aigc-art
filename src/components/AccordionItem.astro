---
import { Heading } from 'accessible-astro-components'

/**
 * Accordion Item Component
 *
 * @description An individual accordion item that can be expanded/collapsed with smooth animations
 */

interface Props {
  /**
   * Additional classes to apply to the item
   */
  class?: string
  /**
   * Title text displayed in the accordion header
   */
  title: string
  /**
   * Optional name attribute for the details element
   */
  name?: string
  /**
   * Heading level for the title (h1-h6)
   * @default 'h3'
   */
  headingLevel?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6'
  /**
   * Visual size of the heading (can differ from semantic level)
   * @default 'h6'
   */
  headingSize?: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6'
  /**
   * Visual style variant of the accordion
   * @default 'default'
   */
  variant?: 'default' | 'chevron'
  /**
   * Whether the accordion item is initially open
   * @default false
   */
  open?: boolean
  /**
   * HTML attributes to spread on the accordion item
   */
  [key: string]: string | number | boolean | undefined
}

const {
  class: className,
  title,
  name,
  headingLevel = 'h3',
  headingSize = 'h6',
  variant = 'default',
  open = false,
  ...rest
} = Astro.props
---

<li class:list={['item', className]} {...rest}>
  <details class:list={['wrapper', `variant-${variant}`]} name={name} {...open ? { open } : {}}>
    <summary class="title">
      <Heading level={headingLevel} size={headingSize}>
        {
          variant === 'chevron' ? (
            <>
              {title}
              <span class="icon-chevron">
                <svg
                  aria-hidden="true"
                  width="12"
                  height="8"
                  fill="none"
                  xmlns="http://www.w3.org/2000/svg"
                >
                  <path
                    d="M1.4 1.6L6 6.2l4.6-4.6"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                </svg>
              </span>
            </>
          ) : (
            <>
              <span class="icon-plus">
                <svg
                  aria-hidden="true"
                  width="20"
                  height="2"
                  fill="none"
                  xmlns="http://www.w3.org/2000/svg"
                >
                  <path fill="currentColor" d="M20 .5v2H0v-2z" />
                </svg>
                <svg width="2" height="20" fill="none" xmlns="http://www.w3.org/2000/svg">
                  <path fill="currentColor" d="M1.5 20h-2V0h2z" />
                </svg>
              </span>
              {title}
            </>
          )
        }
      </Heading>
    </summary>
    <div class="content">
      <slot />
    </div>
  </details>
</li>

<style>
  :where(.wrapper) {
    --transition-duration: 0.3s;
    --transition-easing: cubic-bezier(0.165, 0.84, 0.44, 1);

    /* border-block-start: 2px solid var(--link-color); */
    padding-block: 0;
  }

  .title :global(.heading) {
    display: flex;
    align-items: center;
    gap: 1rem;
    min-inline-size: 100%;
    font-weight: 400;
    font-size: 1.3rem;
    padding-left: 1rem
  }
  .title :global(.heading):hover {
    color: var(--link-hover-color);
  }

  @media (prefers-reduced-motion: no-preference) {
    :where(.wrapper) {
      interpolate-size: allow-keywords;
    }
  }

  details::details-content {
    opacity: 0;
    transition: content-visibility var(--transition-duration) allow-discrete,
      opacity var(--transition-duration) var(--transition-easing),
      block-size var(--transition-duration) var(--transition-easing);
    block-size: 0;
    overflow-y: clip;
  }

  details[open]::details-content {
    opacity: 1;
    block-size: auto;
  }

  .wrapper.variant-default .content {
    padding: 0 0 1.5rem 2.2rem;
  }

  .wrapper.variant-chevron .title :global(.heading) {
    justify-content: space-between;
  }

  .wrapper.variant-chevron .content {
    padding-block-end: 2.2rem;
  }

  :where(.title) {
    display: flex;
    cursor: pointer;
    padding-block: 0.75rem;
    list-style: none;
    background-color: inherit;
  }

  :where(.title)::marker,
  :where(.title)::-webkit-details-marker {
    display: none;
    content: '';
  }

  /* :where(.title:hover),
  :where(.title:focus-visible) {
    color: var(--link-hover-color);
    text-decoration: underline;
    text-underline-offset: 4px;
  } */

  :where(.icon-plus) {
    display: inline-block;
    position: relative;
    aspect-ratio: 1;
    block-size: 20px;
  }

  :where(.icon-plus svg) {
    position: absolute;
  }

  :where(.icon-plus svg:first-of-type) {
    transform: translateY(-50%);
    inset-block-start: 50%;
    inset-inline-start: 0;
  }

  :where(.icon-plus svg:last-of-type) {
    transform: translateY(-50%);
    inset-block-start: 50%;
    inset-inline-start: 50%;
  }

  @media (prefers-reduced-motion: no-preference) {
    :where(.icon-plus svg:last-of-type) {
      transition: all var(--transition-duration) var(--transition-easing);
    }
  }

  :where(.icon-chevron) {
    flex-shrink: 0;
  }

  @media (prefers-reduced-motion: no-preference) {
    :where(.icon-chevron svg) {
      transition: transform var(--transition-duration) var(--transition-easing);
    }
  }

  :where(.content a) {
    display: inline-block;
  }

  :where(.content > *:not(:first-child)) {
    margin-block-start: 0.75rem;
  }

  :where(.wrapper[open] .icon-plus svg:last-of-type) {
    transform: rotate(90deg);
    inset-block-start: 0;
  }

  :where(.wrapper[open] .icon-chevron svg) {
    transform: scaleY(-1);
  }

  .item {
    border-radius: 0.5rem;
    overflow: hidden;
    margin-block: 0.02rem;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    transition: box-shadow 0.3s ease;
  }
  .item:hover {
    box-shadow: 0 6px 16px rgba(0,0,0,0.2);
  }

  .item:nth-child(1) .wrapper { background-color: hsl(200, 80%, 95%); }
  .item:nth-child(2) .wrapper { background-color: hsl(240, 80%, 95%); }
  .item:nth-child(3) .wrapper { background-color: hsl(280, 80%, 95%); }
  .item:nth-child(4) .wrapper { background-color: hsl(320, 80%, 95%); }
  .item:nth-child(5) .wrapper { background-color: hsl(0,   80%, 95%); }
  .item:nth-child(6) .wrapper { background-color: hsl(40,  80%, 95%); }
  .item:nth-child(7) .wrapper { background-color: hsl(80,  80%, 95%); }
  .item:nth-child(8) .wrapper { background-color: hsl(200, 80%, 95%); }
  .item:nth-child(9) .wrapper { background-color: hsl(240, 80%, 95%); }
  .item:nth-child(10) .wrapper { background-color: hsl(280, 80%, 95%); }
  .item:nth-child(11) .wrapper { background-color: hsl(320, 80%, 95%); }
  .item:nth-child(12) .wrapper { background-color: hsl(0,   80%, 95%); }
  .item:nth-child(13) .wrapper { background-color: hsl(40,  80%, 95%); }
  .item:nth-child(14) .wrapper { background-color: hsl(80,  80%, 95%); }


  .item:nth-child(1) .wrapper .content { background-color: hsl(200, 80%, 98%); }
  .item:nth-child(2) .wrapper .content { background-color: hsl(240, 80%, 98%); }
  .item:nth-child(3) .wrapper .content { background-color: hsl(280, 80%, 98%); }
  .item:nth-child(4) .wrapper .content { background-color: hsl(320, 80%, 98%); }
  .item:nth-child(5) .wrapper .content { background-color: hsl(0,   80%, 98%); }
  .item:nth-child(6) .wrapper .content { background-color: hsl(40,  80%, 98%); }
  .item:nth-child(7) .wrapper .content { background-color: hsl(80,  80%, 98%); }
  .item:nth-child(8) .wrapper .content { background-color: hsl(200, 80%, 98%); }
  .item:nth-child(9) .wrapper .content { background-color: hsl(240, 80%, 98%); }
  .item:nth-child(10) .wrapper .content { background-color: hsl(280, 80%, 98%); }
  .item:nth-child(11) .wrapper .content { background-color: hsl(320, 80%, 98%); }
  .item:nth-child(12) .wrapper .content { background-color: hsl(0,   80%, 98%); }
  .item:nth-child(13) .wrapper .content { background-color: hsl(40,  80%, 98%); }
  .item:nth-child(14) .wrapper .content { background-color: hsl(80,  80%, 98%); }

</style>
